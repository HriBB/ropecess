/**
 * PROTOTYPE — extra homepage section data (services, spacenet, contact).
 * Content lives in the shared content module; this file re-exports for
 * backward compatibility with the variant components.
 * Deleted with the prototype — the winning structure gets folded into
 * home.tsx properly.
 */
export { extra, serviceHref } from '~/content/home'
export type { ProtoExtra } from '~/content/home'
